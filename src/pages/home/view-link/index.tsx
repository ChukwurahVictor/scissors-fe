import { Box, Text } from "@chakra-ui/react"
// import { useParams } from "react-router-dom"
// import { useFetchSingleUrl } from "services/swr/link"

const ViewLink = () => {
  // const { id } = useParams();
  // const { data } = useFetchSingleUrl(id);
  // console.log(data);

  return (
    <Box maxW="4xl" mx="auto">
      <Text>Title</Text>
      <Text></Text>
    </Box>
  );
}

export default ViewLink